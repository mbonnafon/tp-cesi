name: essai-github-actoins
on: [ pull_request ]
jobs:
    ci:
        runs-on: ubuntu-latest
        steps:
            -   uses: actions/checkout@v3
            -   run: go test
            -   run: go run main.go
            -   run: go build -o server
